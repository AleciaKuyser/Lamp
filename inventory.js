
// either have the click function branch, or use HTML for drawing inventory
const menu = { rowsize:10, colsize:10, maxrows:10, maxcols:4 }

let overlayInventory = () => {
    let row = 0, col = 0
    // draw each item in the menu
}

let overlayInvItem = () => {
    // make a div for the item
    // display item icon
    // button for use
        // button.mousePressed(useItem(button.value()))
    // button for equip left
        // button.mousePressed(equip(button.value(), "left"))
    // button for equip right
        // button.mousePressed(equip(button.value(), "right"))
}
